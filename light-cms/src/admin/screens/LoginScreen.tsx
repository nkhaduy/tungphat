export function LoginScreen({ onLogin, error, busy }: { onLogin: () => void; error: string; busy: boolean }) {
  return <main className="login-page"><section className="login-panel" aria-labelledby="login-title"><div className="login-brand"><img src="/logo-horizontal.png" alt="Tùng Phát" /></div><p className="login-kicker">LIGHT CMS</p><h1 id="login-title">Đăng nhập</h1><p className="login-description">Quản trị nội dung website Tùng Phát bằng tài khoản Báo Giá nội bộ.</p>{error ? <p className="form-error" role="alert">{error}</p> : null}<button type="button" className="button primary login-button" disabled={busy} onClick={onLogin}>{busy ? "Đang kiểm tra phiên…" : "Đăng nhập bằng tài khoản Báo Giá"}</button><p className="login-note">Chỉ tài khoản quản trị đang hoạt động trong Báo Giá được phép truy cập.</p></section></main>;
}
