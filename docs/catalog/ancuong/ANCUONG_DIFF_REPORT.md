# An Cuong Full Dataset Diff Report

The first full export compared the previous seven-product sample with the completed catalogue: 2,675 `NEW`, 7 `UNCHANGED`, and zero other classifications.

The second cache-only canonical run produced:

- `NEW`: 0.
- `UPDATED`: 0.
- `UNCHANGED`: 2,682.
- `MISSING_FROM_SOURCE`: 0.
- `INVALID`: 0.
- `DUPLICATE`: 0.
- `RELATION_CHANGED`: 0.
- `MEDIA_CHANGED`: 0.

Checksums for normalized catalogue/categories/taxonomy/relations/media and all six export artifacts were identical before and after the second run. Canonical dataset diff: none. Runtime reports may change only when their factual counts change; canonical exports do not churn from timestamps, object order, formatting, absolute paths, or random UUIDs.
