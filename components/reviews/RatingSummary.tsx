import { ExternalLink, Star } from "lucide-react";
import type { ReviewBranch } from "./google-review-types";

export function RatingSummary({ branch }: { branch: ReviewBranch }) {
  const rating = branch.averageRating > 0 ? branch.averageRating.toFixed(1) : "—";
  return <aside className="flex min-h-[287px] w-full flex-col justify-between rounded-[0.9rem] border border-forest-900/10 bg-white p-5 shadow-[0_8px_24px_rgba(7,59,40,.04)]"><div><span aria-hidden="true" className="grid h-9 w-9 place-items-center rounded-full bg-[#f2f5f3] text-sm font-black text-[#4285f4]">G</span><p className="mt-4 text-sm font-extrabold text-forest-950">{branch.location}</p><div className="mt-5 text-4xl font-extrabold tracking-tight text-forest-950">{rating}</div><div className="mt-1 flex items-center gap-1" aria-label={`${rating} trên 5 sao`}><span className="flex gap-0.5">{Array.from({ length: 5 }, (_, index) => <Star key={index} size={14} fill={branch.averageRating >= index + 1 ? "currentColor" : "none"} className={branch.averageRating >= index + 1 ? "text-wood-500" : "text-slate-300"} aria-hidden="true" />)}</span><span className="text-xs text-slate-500">{branch.count ? `${branch.count} đánh giá` : "Chưa có đánh giá"}</span></div></div>{branch.mapsUrl ? <a href={branch.mapsUrl} target="_blank" rel="noopener noreferrer" className="pressable inline-flex min-h-11 items-center justify-center gap-2 rounded-md border border-forest-900/15 bg-white px-3 text-xs font-extrabold text-forest-900 hover:border-wood-500/50">Xem trên Google <ExternalLink size={13} aria-hidden="true" /></a> : null}</aside>;
}
